# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Node (for examples)
node_modules/

# Secrets (never commit these)
*.key
*.pem
*.env

# Cookie Share — local private config (never commit)
config.local.json
extension/manifest.json
extension/local-config.json

# Session build artifacts / backups
build/
extension-key.pem
